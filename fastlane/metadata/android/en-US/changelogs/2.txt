- Search field improvements:
  - Supporting `#`, `@`, and `&`.
  - Special filter logic "FiledUnder": one character only as uppercase
- "Last seen" field:
  - using a date picker
  - new menu item: "last seen" elements.
- UI enhancements:
  - new menu item: elements with no movie poster.
  - added loading screen when gathering the statistics summary.
- Sync improvements
- Sync and import now as foreground services.
- App now checks if notifications for foreground services are enabled
